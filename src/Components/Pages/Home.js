import React, { Fragment } from 'react'
import Header from '../Header'
import SideBar from '../SideBar'

const Home = () => {
   return (
      <Fragment>
         <Header></Header>
         <SideBar></SideBar>
      </Fragment>
   )
}

export default Home
